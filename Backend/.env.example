# Thin fallback for standalone `npm run start:dev` from this directory.
# The root .env (../../.env) is the single source of truth for all ports and credentials.
# This file only overrides DATABASE_HOST for local development (Docker uses "db" hostname).
DATABASE_HOST=localhost
